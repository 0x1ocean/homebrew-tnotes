# Homebrew tap for tnotes

```sh
brew install 0x1ocean/tnotes/tnotes
```

Prebuilt binaries for macOS (Apple Silicon, Intel) and Linux (x86_64, aarch64). The formula is regenerated by the tnotes release workflow on every tag; do not edit by hand — see `scripts/brew-formula.sh` in the main repo.
